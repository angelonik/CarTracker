@inherits PagerModel
<ul class="pagination">
    @*<li class="page-item">
        <a class="page-link" href="/users/1" aria-label="Previous">
            <span aria-hidden="true">&laquo;</span>
            <span class="sr-only">Start</span>
        </a>
    </li>
    <li class="page-item">
        <a class="page-link" href="@($"/users/{(Model.CurrentPage-1).ToString()}")" aria-label="Previous">
            <span aria-hidden="true">&lsaquo;</span>
            <span class="sr-only">Previous</span>
        </a>
    </li>*@
    @for (var i = Model.CurrentPage; Last(i); i++)
    {
        <li class="page-item"><button class="page-link" onclick="@(()=>PagerButtonClicked(2))">2</button></li>
    }

    @*<li class="page-item">
        <a class="page-link" href="@($"/users/{(Model.CurrentPage+1).ToString()}")" aria-label="Next">
            <span aria-hidden="true">&rsaquo;</span>
            <span class="sr-only">Next</span>
        </a>
    </li>
    <li class="page-item">
        <a class="page-link" href="@($"/users/{Model.PageCount.ToString()}")" aria-label="Next">
            <span aria-hidden="true">&raquo;</span>
            <span class="sr-only">End</span>
        </a>
    </li>*@
</ul>